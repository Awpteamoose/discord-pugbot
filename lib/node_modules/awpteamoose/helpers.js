"use strict";

Object.defineProperty(exports, "__esModule", {
	value: true
});
const randomIndex = exports.randomIndex = arr => {
	return Math.floor(Math.random() * arr.length);
};
const pickRandom = exports.pickRandom = arr => arr[randomIndex(arr)];
const delay = exports.delay = ms => {
	return new Promise((resolve, reject) => {
		setTimeout(resolve, ms);
	});
};
const shuffle = exports.shuffle = arr => {
	for (let i = arr.length - 1; i > 0; i -= 1) {
		const j = Math.floor(Math.random() * (i + 1));
		const temp = arr[i];
		arr[i] = arr[j];
		arr[j] = temp;
	}
	return arr;
};
const pickFirst = exports.pickFirst = arr => arr[0];
const logReturn = exports.logReturn = (message, ...optionalParams) => {
	console.log(message, ...optionalParams);return message;
}; // eslint-disable-line
//# sourceMappingURL=helpers.js.map